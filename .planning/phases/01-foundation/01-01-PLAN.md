---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, src/index.ts, .gitignore, Dockerfile, server.js]
autonomous: true
must_haves:
  truths:
    - "Server starts up using npm scripts"
    - "Existing /generate endpoint functions correctly in TS"
    - "Project compiles via tsc"
  artifacts:
    - path: "package.json"
      provides: "Dependency management and scripts"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
    - path: "src/index.ts"
      provides: "Main entry point and migrated logic"
  key_links:
    - from: "src/index.ts"
      to: "gemini CLI"
      via: "execFile"
---

<objective>
Initialize the project with TypeScript and migrate the existing server logic from JavaScript.

Purpose: Provide a modern, type-safe foundation for future feature development.
Output: A working TypeScript-based Express server with proper dependency management.
</objective>

<execution_context>
@/home/piotreee/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server.js
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Initialize package.json and install dependencies</name>
  <files>package.json, .gitignore</files>
  <action>
    Create a package.json with the following:
    - Dependencies: express, cors, dotenv
    - Dev Dependencies: typescript, @types/node, @types/express, @types/cors, ts-node-dev, rimraf
    - Scripts: 
      - "dev": "ts-node-dev --respawn src/index.ts"
      - "build": "rimraf dist && tsc"
      - "start": "node dist/index.js"
    
    Update .gitignore to include node_modules, dist, and .env.
  </action>
  <verify>Run `npm install` and check if node_modules exists and scripts are present.</verify>
  <done>package.json is configured and dependencies are installed.</done>
</task>

<task type="auto">
  <name>Setup TypeScript and migrate server logic</name>
  <files>tsconfig.json, src/index.ts, Dockerfile</files>
  <action>
    - Create tsconfig.json with standard Node.js/TS settings (target: ESNext, module: CommonJS, outDir: ./dist, rootDir: ./src).
    - Create src/index.ts and migrate logic from server.js. 
    - Use proper TypeScript types for req/res and error objects.
    - Ensure CORS middleware is added.
    - Update Dockerfile to:
      - Copy package.json and package-lock.json (if exists).
      - Run npm install.
      - Copy source code.
      - Run npm build.
      - Use `CMD ["npm", "start"]`.
  </action>
  <verify>Run `npm run build` to ensure no compilation errors.</verify>
  <done>TypeScript is configured, logic is migrated, and Dockerfile is updated.</done>
</task>

<task type="auto">
  <name>Cleanup and final verification</name>
  <files>server.js</files>
  <action>
    - Delete the original server.js file.
    - Verify that the server starts locally using `npm run dev` (mocking the gemini command if necessary, or just ensuring it listens).
  </action>
  <verify>Check that server.js is gone and `npm run dev` starts the server without errors.</verify>
  <done>Old file removed and server starts correctly.</done>
</task>

</tasks>

<verification>
Check server starts, check TS compilation, check Dockerfile buildability.
</verification>

<success_criteria>
- Project has a valid package.json and tsconfig.json.
- src/index.ts contains the migrated logic.
- `npm run build` succeeds.
- server.js is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
