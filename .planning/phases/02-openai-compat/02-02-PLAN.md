---
phase: 02-openai-compat
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/index.ts, src/services/OpenAIService.ts]
autonomous: true
must_haves:
  truths:
    - "Streaming responses use Server-Sent Events (SSE)"
    - "Chunks follow OpenAI delta format"
    - "Finish reason is included in the final chunk"
---

<objective>
Support streaming responses for /v1/chat/completions.

Purpose: Enable real-time response generation for better UX.
Output: Streaming capability when `stream: true` is provided in the request.
</objective>

<execution_context>
@/home/piotreee/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-openai-compat/02-01-PLAN.md
@src/index.ts
@src/services/OpenAIService.ts
</context>

<tasks>

<task type="auto">
  <name>Implement streaming logic in OpenAIService</name>
  <files>src/services/OpenAIService.ts</files>
  <action>
    - Add support for streaming output from the `gemini` CLI.
    - Transform streaming output to OpenAI-compatible chunks.
  </action>
  <verify>Check chunk formatting logic.</verify>
  <done>Streaming mapping implemented.</done>
</task>

<task type="auto">
  <name>Update endpoint for streaming</name>
  <files>src/index.ts</files>
  <action>
    - Update /v1/chat/completions to handle `stream: true`.
    - Set appropriate headers for SSE (text/event-stream).
    - Pipe CLI output to the response in real-time.
  </action>
  <verify>Test with curl: `curl -N -X POST http://localhost:3000/v1/chat/completions -H "Content-Type: application/json" -d '{"model": "gemini-pro", "messages": [{"role": "user", "content": "Write a long story"}], "stream": true}'`</verify>
  <done>Streaming support complete.</done>
</task>

</tasks>
