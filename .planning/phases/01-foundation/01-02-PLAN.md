---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/index.ts, src/services/SessionService.ts, src/middleware/errorHandler.ts]
autonomous: true
must_haves:
  truths:
    - "GET /sessions returns a list of session metadata"
    - "GET /sessions/:id returns full chat history"
    - "404 is returned if session file doesn't exist"
  artifacts:
    - path: "src/services/SessionService.ts"
      provides: "Logic for reading session files"
---

<objective>
Implement Session Management API.

Purpose: Allow users to list and retrieve chat histories from the Gemini CLI storage.
Output: Working /sessions and /sessions/:id endpoints.
</objective>

<execution_context>
@/home/piotreee/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-foundation-CONTEXT.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Implement SessionService</name>
  <files>src/services/SessionService.ts</files>
  <action>
    Create a service that:
    - Scans `/home/node/.gemini/tmp/*/chats/*.json` for session files.
    - `listSessions()`: Returns metadata (id, startTime, lastUpdated).
    - `getSession(id)`: Reads and returns the full content of the specific session file.
  </action>
  <verify>Check if file reading logic correctly handles multiple subdirectories.</verify>
  <done>Service implemented.</done>
</task>

<task type="auto">
  <name>Implement API Endpoints and Error Handling</name>
  <files>src/index.ts, src/middleware/errorHandler.ts</files>
  <action>
    - Add endpoints to `src/index.ts`.
    - Implement a basic global error handler middleware.
    - Ensure 404 is sent if `getSession` fails to find a file.
  </action>
  <verify>Test endpoints with CURL.</verify>
  <done>Endpoints active.</done>
</task>

</tasks>
