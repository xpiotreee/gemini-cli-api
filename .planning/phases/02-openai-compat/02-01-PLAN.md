---
phase: 02-openai-compat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/index.ts, src/services/OpenAIService.ts]
autonomous: true
must_haves:
  truths:
    - "Endpoint /v1/chat/completions is available"
    - "Role mapping maps 'assistant' to 'model'"
    - "System messages are mapped to system_instruction"
    - "Consecutive messages with same role are merged"
    - "Response matches OpenAI format"
  artifacts:
    - path: "src/services/OpenAIService.ts"
      provides: "Logic for mapping OpenAI requests to Gemini CLI commands"
---

<objective>
Implement the core OpenAI-compatible /v1/chat/completions endpoint.

Purpose: Allow tools designed for OpenAI to use this API.
Output: A functional POST /v1/chat/completions endpoint.
</objective>

<execution_context>
@/home/piotreee/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/research/OPENAI_COMPAT.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Create OpenAIService</name>
  <files>src/services/OpenAIService.ts</files>
  <action>
    Implement logic to transform OpenAI request to Gemini CLI arguments.
    - Map 'system' role to system instruction (Gemini CLI -s flag).
    - Map 'user' and 'assistant' roles.
    - Implement role alternation (merging consecutive same-role messages).
    - Map parameters (temperature, max_tokens -> max_output_tokens).
  </action>
  <verify>Write a unit test or manual check for the mapping logic.</verify>
  <done>Mapping logic implemented.</done>
</task>

<task type="auto">
  <name>Expose /v1/chat/completions</name>
  <files>src/index.ts</files>
  <action>
    Add POST /v1/chat/completions endpoint to src/index.ts.
    Use OpenAIService to call the gemini CLI and format the response.
  </action>
  <verify>Test with curl: `curl -X POST http://localhost:3000/v1/chat/completions -H "Content-Type: application/json" -d '{"model": "gemini-pro", "messages": [{"role": "user", "content": "Hello"}]}'`</verify>
  <done>Endpoint exposed and functional.</done>
</task>

</tasks>
