---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, src/index.ts, .gitignore, Dockerfile, server.js]
autonomous: true
must_haves:
  truths:
    - "Server starts up using npm scripts"
    - "Existing /generate endpoint functions correctly in TS"
    - "Project compiles via tsc"
  artifacts:
    - path: "package.json"
      provides: "Dependency management and scripts"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
    - path: "src/index.ts"
      provides: "Main entry point and migrated logic"
---

<objective>
Initialize the project with TypeScript and migrate the existing server logic.

Purpose: Provide a modern, type-safe foundation.
Output: A working TypeScript-based Express server.
</objective>

<execution_context>
@/home/piotreee/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server.js
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Initialize package.json and dependencies</name>
  <files>package.json, .gitignore</files>
  <action>
    Create package.json with dependencies (express, cors, dotenv) and devDependencies (typescript, @types/node, @types/express, @types/cors, ts-node-dev, rimraf, glob, @types/glob).
    Scripts: dev, build, start.
    Update .gitignore.
  </action>
  <verify>Run `npm install` and check package.json.</verify>
  <done>Dependencies installed.</done>
</task>

<task type="auto">
  <name>Setup TypeScript and migrate logic</name>
  <files>tsconfig.json, src/index.ts, Dockerfile</files>
  <action>
    - Create tsconfig.json (strict: true).
    - Migrate server.js to src/index.ts.
    - Update Dockerfile for TS (npm install, npm build, CMD ["npm", "start"]).
  </action>
  <verify>Run `npm run build`.</verify>
  <done>TS setup complete.</done>
</task>

<task type="auto">
  <name>Cleanup</name>
  <files>server.js</files>
  <action>Delete server.js.</action>
  <verify>Check file is deleted.</verify>
  <done>Cleanup complete.</done>
</task>

</tasks>
