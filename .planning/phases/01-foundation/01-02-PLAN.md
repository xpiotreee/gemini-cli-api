---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/index.ts, src/middleware/auth.ts, src/middleware/errorHandler.ts, .env.example, README.md]
autonomous: true
must_haves:
  truths:
    - "Requests without Bearer token return 401"
    - "Requests with valid Bearer token return 200 (or other success/logic error)"
    - "Errors are returned as JSON with 'error' and 'details' fields"
  artifacts:
    - path: "src/middleware/auth.ts"
      provides: "Authentication enforcement"
    - path: "src/middleware/errorHandler.ts"
      provides: "Standardized error responses"
  key_links:
    - from: "src/index.ts"
      to: "src/middleware/auth.ts"
      via: "app.use()"
    - from: "src/index.ts"
      to: "src/middleware/errorHandler.ts"
      via: "app.use() at end"
---

<objective>
Implement authentication and standardize error handling.

Purpose: Secure the API and ensure consistent error reporting for clients.
Output: Auth and error middleware integrated into the application.
</objective>

<execution_context>
@/home/piotreee/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-PLAN.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Standardized Error Handling</name>
  <files>src/middleware/errorHandler.ts, src/utils/errors.ts</files>
  <action>
    - Create a custom `ApiError` class in `src/utils/errors.ts` that extends `Error` and includes a `statusCode`.
    - Create `src/middleware/errorHandler.ts` that catches errors and returns a JSON response: `{ error: message, details: details }`.
    - It should handle both `ApiError` and generic `Error` (returning 500 for generic).
  </action>
  <verify>Import and use the middleware in a dummy route and check response.</verify>
  <done>Error handling middleware is implemented.</done>
</task>

<task type="auto">
  <name>Implement Bearer Authentication</name>
  <files>src/middleware/auth.ts, .env.example</files>
  <action>
    - Create `src/middleware/auth.ts` that:
      - Reads `API_AUTH_TOKEN` from environment.
      - Checks `Authorization` header for `Bearer <token>`.
      - Returns 401 if missing or invalid.
    - Create `.env.example` with `API_AUTH_TOKEN` placeholder.
  </action>
  <verify>Test with a dummy route using valid and invalid tokens.</verify>
  <done>Authentication middleware is implemented.</done>
</task>

<task type="auto">
  <name>Integrate middleware and protect routes</name>
  <files>src/index.ts, README.md</files>
  <action>
    - Import and use `errorHandler` and `auth` in `src/index.ts`.
    - Ensure `auth` is applied before the `/generate` route.
    - Ensure `errorHandler` is applied after all routes.
    - Update README.md with instructions on setting `API_AUTH_TOKEN` and how to use it in requests.
  </action>
  <verify>Perform a CURL request to `/generate` without token (expect 401) and with token (expect 200/400 validation error).</verify>
  <done>API is secured and error handling is active.</done>
</task>

</tasks>

<verification>
Check 401 on unauthorized access, check JSON error structure on invalid input.
</verification>

<success_criteria>
- Unauthenticated requests are rejected with 401.
- Valid requests work as before (if token provided).
- Errors follow the standard `{ error: string, details?: any }` format.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
